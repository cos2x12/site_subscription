site_subscription.form:
  path: '/subscription'
  defaults:
    _title: 'Site subscription.'    
    _form: '\Drupal\site_subscription\Form\SubscriptionForm'
  requirements:
    _permission: 'access content'

site_subscription.admin:
  path: '/admin/config/kvantstudio/subscription'
  defaults:
    _title: 'Subscription settings'
    _form: '\Drupal\site_subscription\Form\SubscriptionSettingsForm'            
  requirements:
    _permission: 'access administration pages'

# site_subscription.subscription_number_items:
#   path: '/admin/config/kvantstudio/subscription/subscription-number-items'
#   defaults:
#     _title: 'Subscription number items'
#     _form: '\Drupal\site_subscription\Form\SubscriptionNumberItemsForm'    
#   requirements:
#     _permission: 'access administration pages'    